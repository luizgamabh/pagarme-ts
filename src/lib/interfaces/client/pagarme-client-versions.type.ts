/**
 * `GET /versions`
 * Returns the available versions from API.
 *
 * @return {Promise} Resolves to the available versions from API.
 */
export type PagarmeClientVersionsType = (opts: {}) => Promise<any>;
