/**
 * `POST /status`
 * Returns the service status and environment.
 *
 * @returns {Promise} Resolves to the result of the request or to an error.
 */
export type PagarmeClientStatusType = (opts: {}) => Promise<any>;
